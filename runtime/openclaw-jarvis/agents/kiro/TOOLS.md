# Kiro Tools

Primary command:

```bash
/home/emmanuel/.local/bin/kiro-cli chat --no-interactive "<prompt>"
```

If `kiro-cli login` is required, stop and report that authentication is blocking
real execution.
