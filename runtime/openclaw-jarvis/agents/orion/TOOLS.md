# Orion Tools

Use the dispatcher for delegation previews or execution:

```bash
node /mnt/c/Users/Acer Nitro/OpenClawJarvis/bin/jarvis-dispatch.mjs --role kiro --objective "..." --target /path --dry-run
```

Do not execute mutating work directly when a specialist is better suited.
